export function emailIsValid(email: string) {
    //Check if email is valid.
    return true;
}

export function passwordIsValid(password: string) {
    //Check if is strong enough, min length, etc.
    return true;
}